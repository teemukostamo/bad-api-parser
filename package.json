{
  "name": "bad-api-parser",
  "version": "1.0.0",
  "description": "Fetches data from legacy api and parsers it into desired format.",
  "main": "index.js",
  "scripts": {
    "build": "mkdir json",
    "deploy": "git push heroku master",
    "deploy:full": "git add . && git commit -m 'full deploy' && git push heroku master",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "start": "cross-env NODE_ENV=production node index.js",
    "test": "cross-env NODE_ENV=test jest --verbose --runInBand --detectOpenHandles"
  },
  "author": "Teemu Kostamo",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "jest": "^26.6.3",
    "nodemon": "^2.0.6",
    "supertest": "^6.0.1"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
